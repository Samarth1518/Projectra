# Recommended: comma-separated list of Gemini API keys (rotated on rate limit)
GEMINI_API_KEYS=your_first_key,your_second_key

# Fallback: a single key (used only if GEMINI_API_KEYS is unset)
# GEMINI_API_KEY=your_gemini_api_key_here
